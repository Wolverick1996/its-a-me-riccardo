/** The single hard-coded account both LoginScreen and StartMenu show (photo + name) — Desktop shell chrome, not shared portfolio content, so it doesn't belong in src/content/. Shared here so the two screens can't drift out of sync with each other. */
export const ACCOUNT_NAME = "Riccardo Corona";
export const ACCOUNT_AVATAR = "/user-7.svg";
